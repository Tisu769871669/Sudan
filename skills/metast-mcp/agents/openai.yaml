interface:
  display_name: "Metast MCP"
  short_description: "Fetch Metast data and send service messages"
  default_prompt: "Use $metast-mcp to fetch Metast customer-service data or send a confirmed service message."
